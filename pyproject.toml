[project]
name = "bedrock-agentcore-terraform-deployment"
version = "0.1.0"
description = "Terraform modules for deploying AWS Bedrock AgentCore runtimes"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "boto3>=1.35.0",
]

[dependency-groups]
dev = [
    "pre-commit>=4.0.0",
]
